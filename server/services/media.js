/** Media service (uploads, processing, cleanup). Filled in by M6. */
/** @param {import('../app.js').App} _app @param {import('../app.js').Workspace} _ws */
export function cleanupMedia(_app, _ws) {
  return { removed: 0, bytes: 0 };
}
